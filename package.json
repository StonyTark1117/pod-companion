{
  "name": "pod-companion",
  "version": "1.0.0",
  "description": "ToS-compliant Discord bot that exposes native slash commands and drives the PoD selfbot's localhost control API to stream Plex into voice channels",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "discord.js": "^14.16.0"
  }
}
